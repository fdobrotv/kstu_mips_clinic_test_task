# kstu_mips_clinic_test_task

# To run
    docker-compose down -v
    docker-compose up
    ./gradlew bootRun

# Open in browser
http://localhost:8080/

# To inspect database
    psql -U clinic -h localhost -p 5433